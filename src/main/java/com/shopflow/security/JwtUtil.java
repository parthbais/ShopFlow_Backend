package com.shopflow.security;

import org.springframework.stereotype.Component;

@Component
public class JwtUtil {
    // Phase 4: implement generateToken(), validateToken(), extractEmail()
    // Requires: jjwt-api, jjwt-impl, jjwt-jackson in pom.xml
}
